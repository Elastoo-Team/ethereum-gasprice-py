[tool.poetry]
name = "ethereum-gasprice"
version = "0.1.0"
description = "Tool for fetching actual gasprice for ethereum blockchain"
authors = ["Nikita Yugov <nikitosnikn@yandex.ru>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.25.1"
web3 = "^5.15.0"
eth-utils = "^1.10.0"

[tool.poetry.dev-dependencies]
bumpversion = "^0.6.0"
pre-commit = "^2.10.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
